# computer-use-grid
Precise desktop clicking for AI agents: screenshot grid overlay + empirical coordinate calibration + coords ledger. Fixes high-DPI coordinate drift (150%+ scaling) without touching the driver.
